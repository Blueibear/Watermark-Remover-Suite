[pytest]
markers =
    gpu: requires GPU / model weights (deselect by default)
    integration: hits filesystem/ffmpeg/network (may be slow)
    slow: heavy compute or long-running tests
    windows_only: requires Windows-specific tools (signtool, etc.)
addopts = -ra --tb=short
